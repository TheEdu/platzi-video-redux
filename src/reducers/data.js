const data = (state, action) => {
    switch (action.type) {
        case 'ACTION_LALALA': {
            return state;
        }
        default:
            return state;
    }
}

export default data;